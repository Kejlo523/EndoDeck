# EndoDeck enclosure

The STL files fit the Huawei P8 Lite ALE-L21 deck build. Print the front bezel and rear stand as separate parts, then use the preview renders to confirm orientation before slicing.

These source assets stay versioned. Release binaries are generated into `dist/` and `release/` by CI.
